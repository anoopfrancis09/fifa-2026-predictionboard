import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fifaprediction.board',
  appName: 'FIFA Prediction Board',
  webDir: 'dist'
};

export default config;
